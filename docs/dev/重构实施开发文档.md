# AIsChat 后端重构实施开发文档

> **版本**：v1.0
> **日期**：2026-07-23
> **基于**：重构设计文档 + 现有代码差距分析
> **核心原则**：增量迁移、验证优先、可回退、用户无感

---

## 目录

1. [项目现状与差距分析](#一项目现状与差距分析)
2. [Phase 1：模块化重构（1-2 周）](#二phase-1模块化重构1-2-周)
3. [Phase 2：记忆 Skill 自治 PoC（2-3 周）](#三phase-2记忆-skill-自治-poc2-3-周)
4. [Phase 3：事件总线 + 触发器引擎（3-4 周）](#四phase-3事件总线--触发器引擎3-4-周)
5. [Phase 4：极薄大脑 + 冲突仲裁（4-6 周）](#五phase-4极薄大脑--冲突仲裁4-6-周)
6. [Phase 5：模板系统上线（6-8 周）](#六phase-5模板系统上线6-8-周)
7. [代码复用映射表](#七代码复用映射表)
8. [回退方案与验收标准](#八回退方案与验收标准)
9. [关键风险与应对](#九关键风险与应对)

---

## 一、项目现状与差距分析

### 1.1 现有架构状态

| 模块 | 当前状态 | 代码位置 |
|------|---------|---------|
| **ChatApi** | 已有基础实现，统一接口雏形 | [chat/__init__.py](file:///C:/Users/frank/Documents/AIsChat/AIsChat/backend/app/chat/__init__.py) |
| **事件总线** | 基础 pub/sub，系统事件为主 | [event_bus.py](file:///C:/Users/frank/Documents/AIsChat/AIsChat/backend/app/services/event_bus.py) |
| **Skill 系统** | 被动技能模型（action/inject） | [skill_engine.py](file:///C:/Users/frank/Documents/AIsChat/AIsChat/backend/app/services/skill_engine.py) |
| **行动决策** | 中心化决策，已迁移到 ai/decider | [decider.py](file:///C:/Users/frank/Documents/AIsChat/AIsChat/backend/app/ai/decider.py) |
| **AI 响应 Worker** | 极薄大脑雏形，编排入口 | [response_worker.py](file:///C:/Users/frank/Documents/AIsChat/AIsChat/backend/app/ai/response_worker.py) |
| **LLM 服务** | 已迁移到 ai/llm | [llm_service.py](file:///C:/Users/frank/Documents/AIsChat/AIsChat/backend/app/services/llm_service.py)（兼容层） |

### 1.2 核心差距

| 目标组件 | 差距描述 | 优先级 |
|---------|---------|--------|
| **AutonomousSkill 基类** | 现有被动技能模型 vs 目标自治模型 | 🔴 高 |
| **SkillEventBus** | 基础 pub/sub vs 声明式依赖注入 | 🔴 高 |
| **TriggerEngine** | keyword/regex 触发 vs 六维触发 | 🟡 中 |
| **ConflictArbiter** | 无冲突仲裁机制 | 🟡 中 |
| **ResourceManager** | 无统一资源调度 | 🟡 中 |
| **PersonalityAnchor** | 人格锚点未独立建模 | 🟢 低 |
| **TemplateEngine** | 无模板系统 | 🟢 低 |

---

## 二、Phase 1：模块化重构（1-2 周）

### 2.1 目标

完成模块化准备，为后续拆分铺路。核心产出：
- ChatApi 协议接口完善
- context 声明式化
- group_service 拆分收尾
- 统一发送者序列化

### 2.2 文件级操作清单

#### 2.2.1 ChatApi 接口完善

| 任务 | 文件 | 操作 | 预估工时 | 验收标准 |
|------|------|------|---------|---------|
| 完善 ChatApi 接口定义 | `app/chat/__init__.py` | 补充缺失的 API 方法（`set_member_dnd`, `get_friend_list`, `get_user_info`） | 2h | 所有设计文档中的接口方法均已实现 |
| 定义 ChatApi 协议抽象基类 | `app/chat/protocol.py`（新建） | 创建 `BaseChatApi` ABC，定义标准接口契约 | 3h | 协议接口与设计文档一致 |
| 实现 RPC 适配器预留 | `app/chat/__init__.py` | 添加 `set_transport` 方法，支持未来 RPC 替换 | 2h | 当前 in-process 调用不受影响 |
| 更新路由层使用新接口 | `app/routers/chat.py`（新建） | 基于 ChatApi 实现 REST 端点 | 4h | 群聊/私信 CRUD 接口可用 |

#### 2.2.2 Context 声明式化

| 任务 | 文件 | 操作 | 预估工时 | 验收标准 |
|------|------|------|---------|---------|
| 提取上下文构建逻辑 | `app/ai/llm.py` | 将 `build_messages` 中的硬编码规则抽取为配置 | 4h | context 构建规则可配置 |
| 创建上下文配置模型 | `app/models/context_config.py`（新建） | 定义上下文构建的声明式配置结构 | 2h | 配置模型完整 |
| 实现配置解析器 | `app/services/context_config_parser.py`（新建） | 解析声明式配置并生成上下文 | 3h | 配置可正确应用 |
| 更新 `build_messages` 使用配置 | `app/ai/llm.py` | 替换硬编码逻辑为配置驱动 | 2h | 现有行为不变 |

#### 2.2.3 group_service 拆分收尾

| 任务 | 文件 | 操作 | 预估工时 | 验收标准 |
|------|------|------|---------|---------|
| 迁移 AI 专属逻辑 | `app/services/group_service.py` | 将 `is_ai_only_group`, `pause_notifications`, `resume_and_fetch` 迁移到 `app/ai/` | 2h | `group_service.py` 仅保留 CRUD 和兼容导出 |
| 更新引用路径 | 全局搜索 | 将旧引用更新为新路径 | 2h | 无导入错误 |
| 完善兼容层注释 | `app/services/group_service.py` | 明确标记兼容层，说明废弃计划 | 1h | 注释清晰 |

#### 2.2.4 统一发送者序列化

| 任务 | 文件 | 操作 | 预估工时 | 验收标准 |
|------|------|------|---------|---------|
| 统一 sender 模型 | `app/models/sender.py`（新建） | 定义统一的发送者数据结构 | 2h | 模型覆盖 human/ai/system |
| 更新消息序列化 | `app/utils/message_serializer.py` | 使用统一 sender 模型 | 3h | 发送者信息一致 |
| 更新 ChatApi 接口 | `app/chat/__init__.py` | 参数统一为 sender_type/sender_id | 2h | 接口参数一致 |

### 2.3 关键代码变更

#### ChatApi 协议抽象基类

```python
# app/chat/protocol.py（新建）
from abc import ABC, abstractmethod
from typing import Any, List, Optional
from datetime import datetime


class BaseChatApi(ABC):
    """ChatApi 协议抽象基类 — 聊天世界的统一接口契约"""

    @abstractmethod
    async def create_message(
        self,
        db,
        sender_type: str,
        sender_id: int,
        group_id: Optional[int] = None,
        dm_session_id: Optional[str] = None,
        content: str = "",
        reply_to: Optional[int] = None,
        attachments: Optional[List[str]] = None,
    ) -> dict: ...

    @abstractmethod
    async def list_messages(
        self,
        db,
        group_id: Optional[int] = None,
        dm_session_id: Optional[str] = None,
        limit: int = 50,
        offset: int = 0,
    ) -> List[dict]: ...

    @abstractmethod
    async def set_member_dnd(
        self,
        db,
        member_id: int,
        group_id: int,
        until: Optional[datetime] = None,
        member_type: str = "ai",
    ) -> dict: ...

    @abstractmethod
    async def get_friend_list(self, db, user_id: int) -> List[dict]: ...

    @abstractmethod
    async def get_user_info(self, db, user_id: int) -> dict: ...
```

### 2.4 验收标准

- [ ] ChatApi 接口覆盖设计文档中定义的所有方法
- [ ] `group_service.py` 仅保留 CRUD 和兼容导出，无 AI 专属逻辑
- [ ] 上下文构建支持声明式配置
- [ ] 发送者序列化统一，sender_type/sender_id 一致
- [ ] 现有测试全部通过
- [ ] 用户无感知（API 接口不变）

---

## 三、Phase 2：记忆 Skill 自治 PoC（2-3 周）

### 3.1 目标

实现第一个自治 Skill，验证自治模式可行性。核心产出：
- `AutonomousSkill` 基类
- `SkillEventBus` 事件总线
- `MemorySkill` 自治化
- 消息入口接入事件总线

### 3.2 文件级操作清单

#### 3.2.1 AutonomousSkill 基类设计

| 任务 | 文件 | 操作 | 预估工时 | 验收标准 |
|------|------|------|---------|---------|
| 创建 Skill 基类目录 | `app/skills/`（新建） | 创建目录结构：`__init__.py`, `base.py`, `state_skills/`, `app_skills/` | 1h | 目录结构完整 |
| 实现 AutonomousSkill 基类 | `app/skills/base.py`（新建） | 定义 `should_act`, `act`, `load_state`, `save_state` 方法 | 4h | 基类接口完整 |
| 实现 AppSkill 基类 | `app/skills/base.py` | 继承 AutonomousSkill，添加声明式依赖支持 | 3h | 声明式依赖注入可用 |
| 实现 StateSkill 基类 | `app/skills/base.py` | 继承 AutonomousSkill，添加状态读写接口 | 3h | 状态管理接口完整 |
| 创建 ActDecision / SkillOutput 类型 | `app/skills/base.py` | 定义决策和输出的数据类型 | 2h | 类型定义完整 |

#### 3.2.2 SkillEventBus 实现

| 任务 | 文件 | 操作 | 预估工时 | 验收标准 |
|------|------|------|---------|---------|
| 扩展现有 EventBus | `app/services/skill_event_bus.py`（新建） | 基于 `event_bus.py` 扩展，支持 Skill 间通信 | 4h | 事件订阅/发布可用 |
| 实现声明式依赖注入 | `app/services/skill_event_bus.py` | AppSkill 声明依赖，框架自动注入状态 | 5h | 依赖注入正确工作 |
| 实现事件路由 | `app/services/skill_event_bus.py` | 按事件类型路由到订阅的 Skill | 3h | 事件正确分发 |

#### 3.2.3 MemorySkill 自治化

| 任务 | 文件 | 操作 | 预估工时 | 验收标准 |
|------|------|------|---------|---------|
| 创建 MemorySkill | `app/skills/state_skills/memory_skill.py`（新建） | 将 `store_memory`/`recall_memory` 改造为 State Skill | 6h | 记忆操作可用 |
| 添加 should_act 方法 | `app/skills/state_skills/memory_skill.py` | 实现硬规则决策逻辑 | 3h | 决策逻辑正确 |
| 注册到 SkillEventBus | `app/skills/__init__.py` | 注册 MemorySkill 到事件总线 | 2h | 技能可被事件触发 |

#### 3.2.4 消息入口接入

| 任务 | 文件 | 操作 | 预估工时 | 验收标准 |
|------|------|------|---------|---------|
| 修改消息入口 | `app/ai/response_worker.py` | 在消息处理流程中接入 SkillEventBus | 4h | 消息触发 Skill 执行 |
| 添加 Feature Flag | `app/config.py` | 添加 `ENABLE_AUTONOMOUS_SKILL` 开关 | 1h | 可控制新功能启用 |
| 保留旧逻辑作为兼容 | `app/ai/response_worker.py` | Feature Flag 关闭时使用旧逻辑 | 2h | 关闭时行为不变 |

### 3.3 关键代码变更

#### AutonomousSkill 基类

```python
# app/skills/base.py（新建）
from abc import ABC, abstractmethod
from dataclasses import dataclass, field
from typing import Any, Dict, List, Optional


@dataclass
class ActDecision:
    should_act: bool
    priority: int = 0
    action_type: str = "silent"
    reason: str = ""


@dataclass
class SkillOutput:
    messages_to_send: List[Dict] = field(default_factory=list)
    state_changes: Dict = field(default_factory=dict)
    memory_updates: List[Dict] = field(default_factory=list)
    internal_log: str = ""


class AutonomousSkill(ABC):
    name: str = ""
    description: str = ""
    segment: str = ""
    subscribed_events: List[str] = []

    resource_budget: Dict = {
        "llm_tokens_per_day": 0,
        "messages_per_day": 0,
    }

    @abstractmethod
    async def should_act(self, event: Dict, state: Dict) -> ActDecision: ...

    @abstractmethod
    async def act(self, event: Dict, decision: ActDecision, state: Dict) -> SkillOutput: ...

    async def load_state(self) -> Dict:
        return {}

    async def save_state(self, state: Dict) -> None:
        pass


class AppSkill(AutonomousSkill):
    required_state: Dict = {}

    async def should_act(self, event: Dict, state: Dict) -> ActDecision:
        pass


class StateSkill(AutonomousSkill):
    @abstractmethod
    async def get_state(self, query: Dict) -> Dict: ...

    @abstractmethod
    async def update_state(self, updates: Dict) -> Dict: ...

    async def publish_state_change(self, change: Dict) -> None:
        pass
```

#### SkillEventBus

```python
# app/services/skill_event_bus.py（新建）
from __future__ import annotations
import asyncio
import logging
from typing import Any, Dict, List, Optional
from dataclasses import dataclass, field

from app.services.event_bus import EventBus, event_bus as system_event_bus

logger = logging.getLogger(__name__)


class SkillRegistry:
    """
    技能注册表 — 作为 services 层与 skills 层之间的中介，
    解决循环依赖问题。services 层只依赖 registry 接口，
    不直接依赖 skills 包。
    """
    _instance = None
    _skills: Dict[str, AutonomousSkill] = {}

    def __new__(cls):
        if cls._instance is None:
            cls._instance = super().__new__(cls)
        return cls._instance

    def register(self, skill: AutonomousSkill) -> None:
        self._skills[skill.name] = skill
        logger.debug(f"Skill 注册: {skill.name}")

    def get(self, skill_name: str) -> AutonomousSkill | None:
        return self._skills.get(skill_name)

    def list_skills(self) -> List[str]:
        return list(self._skills.keys())

    def get_all(self) -> List[AutonomousSkill]:
        return list(self._skills.values())


skill_registry = SkillRegistry()


class SkillEventBus:
    def __init__(self, skill_registry: SkillRegistry | None = None):
        self._skill_subscribers: Dict[str, List[str]] = {}
        self._state_providers: Dict[str, str] = {}
        self._event_bus = system_event_bus
        # 依赖注入：通过 SkillRegistry 间接访问 skills，避免循环依赖
        self._skill_registry = skill_registry if skill_registry is not None else SkillRegistry()

    def subscribe(self, event_type: str, skill_name: str) -> None:
        if event_type not in self._skill_subscribers:
            self._skill_subscribers[event_type] = []
        self._skill_subscribers[event_type].append(skill_name)
        logger.debug(f"Skill 订阅事件: {skill_name} -> {event_type}")

    def register_state_provider(self, state_name: str, skill_name: str) -> None:
        self._state_providers[state_name] = skill_name

    async def publish(self, event: Dict) -> None:
        event_type = event.get("type", "")
        subscribers = self._skill_subscribers.get(event_type, [])

        for skill_name in subscribers:
            await self._dispatch_to_skill(skill_name, event)

    async def _dispatch_to_skill(self, skill_name: str, event: Dict) -> None:
        # 通过 SkillRegistry 获取 Skill 实例，避免直接导入 app.skills
        skill = self._skill_registry.get(skill_name)
        if skill is None:
            return

        state = await self._resolve_required_state(skill)
        decision = await skill.should_act(event, state)

        if decision.should_act:
            output = await skill.act(event, decision, state)
            await self._process_output(skill, output)

    async def _resolve_required_state(self, skill) -> Dict:
        state = {}
        for state_name, config in getattr(skill, "required_state", {}).items():
            provider = self._state_providers.get(state_name)
            if provider:
                provider_skill = self._skill_registry.get(provider)
                if provider_skill:
                    state[state_name] = await provider_skill.get_state(config)
        return state

    async def _process_output(self, skill, output: SkillOutput) -> None:
        if output.memory_updates:
            for update in output.memory_updates:
                await self._event_bus.emit("memory_updated", **update)

        if output.messages_to_send:
            from app.chat import chat_api
            for msg in output.messages_to_send:
                await chat_api.create_message(**msg)


skill_event_bus = SkillEventBus()
```

### 3.4 与现有 skill_engine.py 的共存策略

**适配器模式**：不直接替换现有被动技能，而是通过适配器将其包装为 `AutonomousSkill`。

```python
# app/skills/adapters.py（新建）
from app.skills.base import AutonomousSkill, ActDecision, SkillOutput
from app.services.skill_engine import evaluate_action_skills, evaluate_inject_skills


class LegacySkillAdapter(AutonomousSkill):
    """
    遗留技能适配器 — 将现有 action/inject 技能包装为 AutonomousSkill。
    
    过渡期间使用，逐步淘汰。
    """
    def __init__(self, legacy_skill_type: str):
        self.legacy_skill_type = legacy_skill_type
        self.name = f"legacy_{legacy_skill_type}"
        self.description = f"遗留技能适配器: {legacy_skill_type}"

    async def should_act(self, event: Dict, state: Dict) -> ActDecision:
        db = state.get("db")
        agent = state.get("agent")
        group_id = event.get("group_id")

        if self.legacy_skill_type == "action":
            result = await evaluate_action_skills(db, agent, group_id, context=event)
            return ActDecision(
                should_act=result.delay_seconds > 0 or result.show_typing,
                priority=10,
                action_type="internal",
                reason="Legacy action skill triggered",
            )
        elif self.legacy_skill_type == "inject":
            prompts = await evaluate_inject_skills(db, agent, group_id, context=event)
            return ActDecision(
                should_act=len(prompts) > 0,
                priority=5,
                action_type="internal",
                reason="Legacy inject skill triggered",
            )
        return ActDecision(should_act=False)

    async def act(self, event: Dict, decision: ActDecision, state: Dict) -> SkillOutput:
        return SkillOutput()
```

### 3.5 验收标准

- [ ] `AutonomousSkill` 基类接口完整
- [ ] `SkillEventBus` 支持事件订阅/发布和声明式依赖注入
- [ ] `MemorySkill` 可被消息事件触发，正确执行记忆操作
- [ ] 旧技能通过适配器继续工作，行为不变
- [ ] Feature Flag 可控制新功能启用/关闭
- [ ] 现有测试全部通过

---

## 四、Phase 3：事件总线 + 触发器引擎（3-4 周）

### 4.1 目标

实现多维触发器和注意力系统。核心产出：
- `TriggerEngine` 触发器引擎
- `AgentTrigger` 数据模型
- `AgentAttention` 数据模型
- 前置过滤机制

### 4.2 文件级操作清单

#### 4.2.1 触发器引擎

| 任务 | 文件 | 操作 | 预估工时 | 验收标准 |
|------|------|------|---------|---------|
| 创建触发器 ORM | `app/models/agent_trigger.py`（新建） | 定义 `AgentTrigger` 数据模型 | 2h | 模型覆盖六维触发 |
| 创建触发器引擎 | `app/services/trigger_engine.py`（新建） | 实现注册、检查、触发逻辑 | 6h | 触发器可用 |
| 实现时间触发器 | `app/services/trigger_engine.py` | 基于 `alarm_service.py` 改造 | 3h | 时间触发可用 |
| 实现事件触发器 | `app/services/trigger_engine.py` | 基于事件总线 | 2h | 事件触发可用 |
| 实现语义触发器 | `app/services/trigger_engine.py` | 基于向量搜索 | 4h | 语义触发可用 |
| 实现关系/状态/复合触发器 | `app/services/trigger_engine.py` | 实现剩余触发类型 | 4h | 所有类型可用 |

#### 4.2.2 注意力系统

| 任务 | 文件 | 操作 | 预估工时 | 验收标准 |
|------|------|------|---------|---------|
| 创建注意力 ORM | `app/models/agent_attention.py`（新建） | 定义 `AgentAttention` 数据模型 | 2h | 模型完整 |
| 实现注意力系统 | `app/services/attention_system.py`（新建） | 实现兴趣域声明和前置过滤 | 4h | 过滤机制可用 |
| 接入消息入口 | `app/ai/response_worker.py` | 在消息处理前应用注意力过滤 | 3h | 过滤生效 |

#### 4.2.3 新增 Skill

| 任务 | 文件 | 操作 | 预估工时 | 验收标准 |
|------|------|------|---------|---------|
| 创建 subscribe_event Skill | `app/skills/app_skills/subscribe_event.py`（新建） | 订阅事件的 App Skill | 2h | 订阅功能可用 |
| 创建 update_attention Skill | `app/skills/app_skills/update_attention.py`（新建） | 更新注意力设置的 App Skill | 2h | 注意力更新可用 |

### 4.3 关键代码变更

#### TriggerEngine

```python
# app/services/trigger_engine.py（新建）
import logging
from datetime import datetime
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select

from app.models.agent_trigger import AgentTrigger
from app.services.skill_event_bus import skill_event_bus

logger = logging.getLogger(__name__)


class TriggerEngine:
    async def register_trigger(self, db: AsyncSession, agent_id: int, trigger_data: dict) -> AgentTrigger:
        trigger = AgentTrigger(
            agent_id=agent_id,
            trigger_type=trigger_data["trigger_type"],
            task=trigger_data["task"],
            condition=trigger_data.get("condition", {}),
            expires_at=trigger_data.get("expires_at"),
            max_fires=trigger_data.get("max_fires", -1),
        )
        db.add(trigger)
        await db.commit()
        await db.refresh(trigger)
        return trigger

    async def unregister_trigger(self, db: AsyncSession, agent_id: int, trigger_id: int) -> None:
        result = await db.execute(
            select(AgentTrigger).where(
                AgentTrigger.id == trigger_id,
                AgentTrigger.agent_id == agent_id,
            )
        )
        trigger = result.scalar_one_or_none()
        if trigger:
            await db.delete(trigger)
            await db.commit()

    async def check_triggers(self, db: AsyncSession, event: dict) -> list[AgentTrigger]:
        """检查哪些触发器匹配当前事件"""
        matched = []
        event_type = event.get("type")
        
        result = await db.execute(
            select(AgentTrigger).where(
                AgentTrigger.status == "pending",
            )
        )
        triggers = result.scalars().all()

        for trigger in triggers:
            if await self._matches(trigger, event):
                matched.append(trigger)
        
        return matched

    async def _matches(self, trigger: AgentTrigger, event: dict) -> bool:
        """检查单个触发器是否匹配"""
        t_type = trigger.trigger_type
        
        if t_type == "time":
            return self._match_time(trigger, event)
        elif t_type == "event":
            return self._match_event(trigger, event)
        elif t_type == "semantic":
            return await self._match_semantic(trigger, event)
        elif t_type == "relational":
            return self._match_relational(trigger, event)
        elif t_type == "state":
            return self._match_state(trigger, event)
        elif t_type == "composite":
            return await self._match_composite(trigger, event)
        
        return False

    async def fire_trigger(self, db: AsyncSession, trigger: AgentTrigger) -> None:
        """触发触发器"""
        trigger.fire_count += 1
        
        if trigger.max_fires > 0 and trigger.fire_count >= trigger.max_fires:
            trigger.status = "cancelled"
        else:
            trigger.status = "fired"
            
        await db.commit()
        
        await skill_event_bus.publish({
            "type": "trigger_fired",
            "agent_id": trigger.agent_id,
            "trigger_id": trigger.id,
            "task": trigger.task,
        })


trigger_engine = TriggerEngine()
```

### 4.4 验收标准

- [ ] 六维触发器全部实现
- [ ] 注意力前置过滤生效
- [ ] 触发器 CRUD 接口可用
- [ ] 订阅事件和更新注意力 Skill 可用
- [ ] 现有测试全部通过

---

## 五、Phase 4：极薄大脑 + 冲突仲裁（4-6 周）

### 5.1 目标

实现冲突仲裁和资源调度，完成大脑瘦身。核心产出：
- `ConflictArbiter` 冲突仲裁
- `ResourceManager` 资源调度
- 人格锚点注入
- 决策逻辑下放

### 5.2 文件级操作清单

#### 5.2.1 冲突仲裁

| 任务 | 文件 | 操作 | 预估工时 | 验收标准 |
|------|------|------|---------|---------|
| 创建冲突仲裁器 | `app/services/conflict_arbiter.py`（新建） | 实现 `arbitrate` 函数 | 3h | 仲裁逻辑可用 |
| 创建 SpeechRequest 模型 | `app/models/speech_request.py`（新建） | 定义发言请求数据结构 | 2h | 模型完整 |
| 接入 SkillEventBus | `app/services/skill_event_bus.py` | Skill 输出进入仲裁队列 | 3h | 冲突仲裁生效 |

#### 5.2.2 资源调度

| 任务 | 文件 | 操作 | 预估工时 | 验收标准 |
|------|------|------|---------|---------|
| 创建资源管理器 | `app/services/resource_manager.py`（新建） | 实现 LLM/DB/记忆资源调度 | 4h | 资源调度可用 |
| 创建资源配额模型 | `app/models/resource_budget.py`（新建） | 定义配额数据结构 | 2h | 模型完整 |
| 接入 Skill 执行 | `app/skills/base.py` | 在 `act` 方法中检查资源 | 3h | 资源限制生效 |

#### 5.2.3 人格锚点

| 任务 | 文件 | 操作 | 预估工时 | 验收标准 |
|------|------|------|---------|---------|
| 创建人格锚点 ORM | `app/models/personality_anchor.py`（新建） | 定义人格锚点数据模型 | 2h | 模型完整 |
| 实现锚点注入机制 | `app/services/personality_anchor_service.py`（新建） | 注入到系统提示词 | 3h | 锚点注入生效 |
| 更新 build_messages | `app/ai/llm.py` | 使用锚点服务 | 2h | 人格一致性保持 |

#### 5.2.4 决策逻辑下放

| 任务 | 文件 | 操作 | 预估工时 | 验收标准 |
|------|------|------|---------|---------|
| 创建 ChatChainSkill | `app/skills/app_skills/chat_chain_skill.py`（新建） | 对话链管理 | 4h | 对话链逻辑迁移 |
| 创建 AlarmSkill | `app/skills/state_skills/alarm_skill.py`（新建） | 闹钟管理 | 4h | 闹钟逻辑迁移 |
| 创建 DecisionSkill | `app/skills/app_skills/decision_skill.py`（新建） | 意愿决策 | 6h | 决策逻辑迁移 |
| 更新 response_worker | `app/ai/response_worker.py` | 移除下放的逻辑 | 4h | 大脑瘦身完成 |

### 5.3 验收标准

- [ ] 冲突仲裁机制可用，多 Skill 同时发言时正确处理
- [ ] 资源调度器控制 LLM/DB/记忆访问
- [ ] 人格锚点注入生效，AI 身份一致性保持
- [ ] 决策逻辑全部下放给 Skill，大脑代码 < 300 行
- [ ] 现有测试全部通过

---

## 六、Phase 5：模板系统上线（6-8 周）

### 6.1 目标

实现零代码 Skill 创作。核心产出：
- 模板引擎
- 触发-动作模板
- 模板到代码导出
- 模板市场

### 6.2 文件级操作清单

| 任务 | 文件 | 操作 | 预估工时 | 验收标准 |
|------|------|---------|---------|
| 创建模板引擎 | `app/services/template_engine.py`（新建） | 解析和渲染模板 | 6h | 模板引擎可用 |
| 创建触发-动作模板 | `app/templates/trigger_action.py`（新建） | 覆盖 60% 需求的模板 | 4h | 模板可用 |
| 创建角色设定模板 | `app/templates/role_template.py`（新建） | 覆盖 25% 需求 | 3h | 模板可用 |
| 创建工作流模板 | `app/templates/workflow_template.py`（新建） | 覆盖 10% 需求 | 3h | 模板可用 |
| 实现模板到代码导出 | `app/services/template_engine.py` | 生成 App Skill 代码 | 4h | 导出功能可用 |
| 创建模板注册表 | `app/services/template_registry.py`（新建） | 管理模板元数据 | 3h | 注册表可用 |
| 创建模板市场 API | `app/routers/templates.py`（新建） | 模板列表/创建/导出接口 | 4h | API 可用 |

### 6.3 验收标准

- [ ] 三种模板类型全部实现
- [ ] 模板到代码导出功能可用
- [ ] 模板市场 API 完整
- [ ] 用户可零代码创建 Skill
- [ ] 现有测试全部通过

---

## 七、代码复用映射表

| 现有模块 | 复用方式 | 目标位置 | 迁移阶段 |
|---------|---------|---------|---------|
| `event_bus.py` | 作为基础事件分发，扩展为 `SkillEventBus` | `skill_event_bus.py` | Phase 2 |
| `skill_engine.py` | 保留作为兼容层，通过适配器包装旧 Skill | `skills/adapters.py` | Phase 2 |
| `action_decider.py` | 决策逻辑下放给 `DecisionSkill` | `skills/app_skills/decision_skill.py` | Phase 4 |
| `alarm_service.py` | 改造为 `AlarmSkill`（State Skill） | `skills/state_skills/alarm_skill.py` | Phase 4 |
| `chat_chain_service.py` | 改造为 `ChatChainSkill`（App Skill） | `skills/app_skills/chat_chain_skill.py` | Phase 4 |
| `group_service.py` | 保留 CRUD，AI 逻辑迁移 | `chat/` 子模块 | Phase 1 |
| `llm_service.py` | 已迁移到 `app.ai.llm`，保留兼容层 | `app.ai.llm` | 已完成 |
| `ai_response_worker.py` | 逐步瘦身，移除下放逻辑 | `app.ai.response_worker.py` | Phase 1-4 |

---

## 八、回退方案与验收标准

### 8.1 回退方案

| 阶段 | 回退点 | 回退方式 | 回退代价 |
|------|-------|---------|---------|
| Phase 1 | Feature Flag: `ENABLE_CHATAPI_V2` | 关闭 Flag，使用旧接口 | 无 |
| Phase 2 | Feature Flag: `ENABLE_AUTONOMOUS_SKILL` | 关闭 Flag，使用旧 skill_engine | 无 |
| Phase 3 | Feature Flag: `ENABLE_TRIGGER_ENGINE` | 关闭 Flag，使用旧触发逻辑 | 无 |
| Phase 4 | Feature Flag: `ENABLE_THIN_BRAIN` | 关闭 Flag，使用中心化决策 | 无 |
| Phase 5 | Feature Flag: `ENABLE_TEMPLATE_SYSTEM` | 关闭 Flag，不加载模板引擎 | 无 |

### 8.2 验收标准汇总

| 阶段 | 核心验收标准 |
|------|------------|
| Phase 1 | ChatApi 接口完善、context 声明式化、group_service 拆分、发送者序列化统一 |
| Phase 2 | AutonomousSkill 基类、SkillEventBus、MemorySkill 自治化、消息接入 |
| Phase 3 | 六维触发器、注意力系统、subscribe_event/update_attention Skill |
| Phase 4 | 冲突仲裁、资源调度、人格锚点、决策下放、大脑 < 300 行 |
| Phase 5 | 三种模板、模板到代码导出、模板市场 API |

### 8.3 通用验收标准

- [ ] 所有现有测试通过
- [ ] 用户无感知（API 接口不变）
- [ ] 性能不降级（P95 延迟 < 2s）
- [ ] 内存使用不增加超过 10%

---

## 九、数据库迁移策略

### 9.1 迁移方式

项目采用 **Alembic 版本化迁移**作为标准方式，同时保留 `migration.py` 中的幂等迁移函数作为补充。

**规则**：
- 新增数据库变更必须通过 `alembic revision --autogenerate` 生成迁移脚本
- 不要再手动写 `_migrate_xxx` 函数（除非是临时的兼容性迁移）
- 迁移脚本必须幂等，支持重复执行

### 9.2 新增 ORM 模型迁移清单

| 模型 | 阶段 | 表名 | DDL 定义 |
|------|------|------|---------|
| `AgentTrigger` | Phase 3 | `agent_triggers` | 见下方 DDL |
| `AgentAttention` | Phase 3 | `agent_attentions` | 见下方 DDL |
| `PersonalityAnchor` | Phase 4 | `personality_anchors` | 见下方 DDL |
| `SpeechRequest` | Phase 4 | `speech_requests` | 见下方 DDL |
| `ResourceBudget` | Phase 4 | `resource_budgets` | 见下方 DDL |

### 9.3 DDL 定义

#### AgentTrigger 表

```sql
CREATE TABLE agent_triggers (
    id SERIAL PRIMARY KEY,
    agent_id INT NOT NULL REFERENCES agents(id) ON DELETE CASCADE,
    trigger_type VARCHAR(20) NOT NULL 
        CHECK (trigger_type IN ('time', 'event', 'semantic', 'relational', 'state', 'composite')),
    task TEXT NOT NULL,
    status VARCHAR(20) DEFAULT 'pending' 
        CHECK (status IN ('pending', 'fired', 'cancelled')),
    expires_at TIMESTAMPTZ,
    max_fires INT DEFAULT -1,
    fire_count INT DEFAULT 0,
    condition JSONB DEFAULT '{}',
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_agent_triggers_agent ON agent_triggers(agent_id);
CREATE INDEX IF NOT EXISTS idx_agent_triggers_type ON agent_triggers(trigger_type);
CREATE INDEX IF NOT EXISTS idx_agent_triggers_status ON agent_triggers(status);
```

#### AgentAttention 表

```sql
CREATE TABLE agent_attentions (
    id SERIAL PRIMARY KEY,
    agent_id INT NOT NULL REFERENCES agents(id) ON DELETE CASCADE,
    group_id INT REFERENCES groups(id),
    interested_topics TEXT[],
    interested_users INT[],
    interested_patterns TEXT[],
    ignored_topics TEXT[],
    ignored_patterns TEXT[],
    match_action VARCHAR(20) DEFAULT 'highlight' 
        CHECK (match_action IN ('highlight', 'wake', 'silent_remember')),
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW(),
    UNIQUE(agent_id, group_id)
);

CREATE INDEX IF NOT EXISTS idx_agent_attentions_agent ON agent_attentions(agent_id);
CREATE INDEX IF NOT EXISTS idx_agent_attentions_group ON agent_attentions(group_id);
```

#### PersonalityAnchor 表

```sql
CREATE TABLE personality_anchors (
    id SERIAL PRIMARY KEY,
    agent_id INT NOT NULL REFERENCES agents(id) ON DELETE CASCADE UNIQUE,
    name VARCHAR(100) NOT NULL,
    identity TEXT NOT NULL,
    personality TEXT NOT NULL,
    core_values TEXT[],
    consistency_coefficient DOUBLE PRECISION DEFAULT 0.7,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_personality_anchors_agent ON personality_anchors(agent_id);
```

#### SpeechRequest 表

```sql
CREATE TABLE speech_requests (
    id SERIAL PRIMARY KEY,
    agent_id INT NOT NULL REFERENCES agents(id) ON DELETE CASCADE,
    skill_name VARCHAR(100) NOT NULL,
    priority INT DEFAULT 0,
    action_type VARCHAR(20) DEFAULT 'silent' 
        CHECK (action_type IN ('speak', 'remember', 'silent', 'internal')),
    reason TEXT,
    messages JSONB DEFAULT '[]',
    state_changes JSONB DEFAULT '{}',
    memory_updates JSONB DEFAULT '[]',
    status VARCHAR(20) DEFAULT 'pending' 
        CHECK (status IN ('pending', 'approved', 'rejected', 'executed')),
    created_at TIMESTAMP DEFAULT NOW(),
    processed_at TIMESTAMP
);

CREATE INDEX IF NOT EXISTS idx_speech_requests_agent ON speech_requests(agent_id);
CREATE INDEX IF NOT EXISTS idx_speech_requests_status ON speech_requests(status);
CREATE INDEX IF NOT EXISTS idx_speech_requests_priority ON speech_requests(priority DESC);
```

#### ResourceBudget 表

```sql
CREATE TABLE resource_budgets (
    id SERIAL PRIMARY KEY,
    agent_id INT NOT NULL REFERENCES agents(id) ON DELETE CASCADE UNIQUE,
    llm_tokens_per_day INT DEFAULT 100000,
    messages_per_day INT DEFAULT 100,
    memory_reads_per_day INT DEFAULT 1000,
    memory_writes_per_day INT DEFAULT 100,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

CREATE INDEX IF NOT EXISTS idx_resource_budgets_agent ON resource_budgets(agent_id);
```

### 9.4 迁移执行流程

```
1. 编写 ORM 模型 → 2. 生成迁移脚本 → 3. 审查脚本 → 4. 测试执行 → 5. 生产部署
   ↓
   python -m alembic revision --autogenerate -m "add agent_triggers table"
   ↓
   python -m alembic upgrade head
```

---

## 十、tools/ 与 skills/ 关系说明

### 10.1 架构分层

```
┌─────────────────────────────────────────────┐
│              Skills 层（决策层）              │
│  AutonomousSkill / AppSkill / StateSkill    │
│  - 感知事件 → 决策 → 调用工具执行              │
├─────────────────────────────────────────────┤
│              Tools 层（执行层）               │
│  ToolPlugin / ToolRegistry                  │
│  - LLM 调用的执行原语，无决策能力              │
├─────────────────────────────────────────────┤
│              ChatApi 层（接口层）             │
│  ChatApi / ConnectionManager                │
│  - AI 与聊天世界的唯一契约边界                 │
└─────────────────────────────────────────────┘
```

### 10.2 核心区别

| 维度 | Tools（工具层） | Skills（技能层） |
|------|---------------|-----------------|
| **角色** | LLM 调用的执行原语 | 自治决策单元 |
| **能力** | 单一动作执行 | 感知 → 决策 → 执行 |
| **状态** | 无状态 | 有状态（State Skill） |
| **触发** | 被 LLM 调用 | 事件驱动、自主触发 |
| **示例** | `send_message`, `store_memory` | `MemorySkill`, `ChatChainSkill` |

### 10.3 调用关系

```
Skill 执行流程：
  should_act(event, state) → 决策是否行动
      ↓
  act(event, decision, state) → 执行动作
      ↓
  ├── 调用 ChatApi 操作聊天世界（发消息、查消息等）
  ├── 调用 Tools 执行 LLM 工具（store_memory、web_search 等）
  └── 更新 State Skill 状态
```

### 10.4 依赖方向约定

**单向依赖原则**：

```
skills → services → chat → database
     ↖                    ↗
      └────── tools ──────┘
```

| 层级 | 允许依赖 | 禁止依赖 |
|------|---------|---------|
| `skills/` | `services/`（通过 SkillRegistry）、`tools/`、`chat/` | 无 |
| `services/` | `chat/`、`database`、`SkillRegistry` | `skills/`（禁止直接导入） |
| `tools/` | `database`、`chat/` | `skills/`、`services/` |
| `chat/` | `database` | `skills/`、`services/`、`tools/` |

**关键规则**：
- `services/` 层必须通过 `SkillRegistry` 间接访问 `skills/`，禁止 `from app.skills import xxx`
- `skills/` 层可以直接导入 `services/`、`tools/`、`chat/`
- `tools/` 层作为执行原语，不依赖任何上层模块

**循环依赖解决方案**：
- `SkillEventBus` 通过构造函数注入 `SkillRegistry`，而非直接导入 `app.skills`
- `SkillRegistry` 在启动时由 `skills/__init__.py` 注册所有 Skill 实例
- 运行时 `SkillEventBus` 通过 `SkillRegistry.get()` 获取 Skill 实例

### 10.5 代码复用策略

| 现有 Tool | 复用方式 | 对应 Skill |
|----------|---------|-----------|
| `store_memory` | 被 MemorySkill 内部调用 | `MemorySkill` |
| `recall_memory` | 被 MemorySkill 内部调用 | `MemorySkill` |
| `manage_records` | 被 MemorySkill 内部调用 | `MemorySkill` |
| `send_message` | 被 ChatApi 封装，Skill 调用 ChatApi | 所有需要发言的 Skill |
| `send_dm` | 被 ChatApi 封装，Skill 调用 ChatApi | 所有需要发私信的 Skill |
| `set_alarm` | 被 AlarmSkill 内部调用 | `AlarmSkill` |
| `set_dnd` | 被 ChatApi 封装，Skill 调用 ChatApi | `DecisionSkill` |
| `switch_state` | 被 ChatApi 封装，Skill 调用 ChatApi | `DecisionSkill` |

---

## 十一、测试计划

### 11.1 测试策略

采用 **分层测试 + 影子模式** 策略：

| 测试类型 | 覆盖范围 | 工具 |
|---------|---------|------|
| 单元测试 | 单个函数/类 | pytest |
| 集成测试 | 模块间协作 | pytest + asyncio |
| 端到端测试 | 完整业务流程 | pytest + httpx |
| 影子模式 | 新旧系统并行验证 | 自定义框架 |

### 11.2 Phase 1 测试计划

| 测试项 | 类型 | 验收标准 |
|--------|------|---------|
| ChatApi 接口完整性 | 单元测试 | 所有设计文档中的接口方法均已实现 |
| ChatApi 协议兼容性 | 集成测试 | 现有调用方代码无需修改 |
| context 声明式配置 | 单元测试 | 配置修改后上下文构建正确变化 |
| 发送者序列化统一 | 单元测试 | sender_type/sender_id 一致 |
| group_service 拆分 | 集成测试 | AI 逻辑迁移后功能不变 |

### 11.3 Phase 2 测试计划

| 测试项 | 类型 | 验收标准 |
|--------|------|---------|
| AutonomousSkill 基类接口 | 单元测试 | should_act/act/load_state/save_state 方法正确 |
| SkillEventBus 事件订阅/发布 | 单元测试 | 事件正确分发到订阅的 Skill |
| 声明式依赖注入 | 集成测试 | AppSkill 声明的依赖自动注入 |
| MemorySkill 自治化 | 集成测试 | 消息事件触发记忆操作正确执行 |
| 影子模式（新旧 Skill 系统并行） | 端到端测试 | 新旧系统输出一致 |

### 11.4 Phase 3 测试计划

| 测试项 | 类型 | 验收标准 |
|--------|------|---------|
| 时间触发器 | 集成测试 | 定时触发正确 |
| 事件触发器 | 集成测试 | 事件触发正确 |
| 语义触发器 | 集成测试 | 向量搜索匹配正确 |
| 关系/状态/复合触发器 | 集成测试 | 所有触发类型正确 |
| 注意力前置过滤 | 集成测试 | 过滤规则生效 |

### 11.5 Phase 4 测试计划

| 测试项 | 类型 | 验收标准 |
|--------|------|---------|
| 冲突仲裁 | 集成测试 | 多 Skill 同时发言时正确排序 |
| 资源调度器 | 集成测试 | 资源配额控制生效 |
| 人格锚点注入 | 集成测试 | 系统提示词中包含锚点 |
| 决策逻辑下放 | 端到端测试 | 大脑瘦身到 < 300 行，功能不变 |

### 11.6 Phase 5 测试计划

| 测试项 | 类型 | 验收标准 |
|--------|------|---------|
| 触发-动作模板 | 端到端测试 | 零代码创建 Skill 可用 |
| 角色设定模板 | 端到端测试 | 角色模板生成正确 |
| 工作流模板 | 端到端测试 | 多步骤任务流正确执行 |
| 模板到代码导出 | 集成测试 | 导出的代码可运行 |

### 11.7 影子模式验证方案

在 Phase 2-4 期间，采用影子模式验证：新旧系统并行执行，比对关键决策字段。

```python
# app/services/skill_event_bus.py（影子模式）
def _compare_decisions(old_decision: Dict, new_decision: Dict) -> Dict[str, list]:
    """
    比对新旧系统的决策结果，返回差异字段。
    比对关键字段：should_act, priority, action_type, reason
    """
    diffs = {}
    
    compare_fields = ["should_act", "priority", "action_type", "reason"]
    for field in compare_fields:
        old_val = old_decision.get(field)
        new_val = new_decision.get(field)
        
        if old_val != new_val:
            diffs[field] = [old_val, new_val]
    
    return diffs


async def _dispatch_to_skill(self, skill_name: str, event: Dict) -> None:
    skill = self._skill_registry.get(skill_name)
    if skill is None:
        return

    if settings.SHADOW_MODE_ENABLED:
        # 并行执行新旧系统
        old_result = await _run_legacy_handler(skill_name, event)
        new_result = await _run_autonomous_skill(skill, event)
        
        # 结构化字段比对
        old_decision = {
            "should_act": old_result.get("should_act", False),
            "priority": old_result.get("priority", 0),
            "action_type": old_result.get("action_type", "silent"),
            "reason": old_result.get("reason", ""),
        }
        
        new_decision = {
            "should_act": new_result.should_act if hasattr(new_result, "should_act") else False,
            "priority": new_result.priority if hasattr(new_result, "priority") else 0,
            "action_type": new_result.action_type if hasattr(new_result, "action_type") else "silent",
            "reason": new_result.reason if hasattr(new_result, "reason") else "",
        }
        
        diffs = _compare_decisions(old_decision, new_decision)
        if diffs:
            logger.warning(f"🔍 Shadow mode mismatch for {skill_name}:")
            for field, [old_val, new_val] in diffs.items():
                logger.warning(f"  {field}: old={old_val!r} vs new={new_val!r}")
            
        return new_result
    
    return await _run_autonomous_skill(skill, event)
```

### 11.8 性能测试

| 指标 | 基准值 | 验收标准 |
|------|--------|---------|
| P95 延迟 | < 2s | 迁移后不增加 |
| 内存使用 | — | 不增加超过 10% |
| 消息处理吞吐量 | — | 不下降 |

---

## 十二、关键风险与应对

### 12.1 风险清单

| 风险 | 概率 | 影响 | 应对策略 |
|------|------|------|---------|
| AutonomousSkill 设计不当，导致后续扩展困难 | ⭐⭐⭐ | 高 | 先做最小可行接口，预留扩展点 |
| 去中心化后调试困难 | ⭐⭐ | 中 | 声明式依赖让输入输出明确，完善日志 |
| 资源竞争导致死锁 | ⭐⭐ | 中 | 资源调度器使用标准死锁预防策略 |
| 状态同步不一致 | ⭐⭐ | 中 | State Skill 唯一真实来源 + 事件溯源 |
| 复杂度失控 | ⭐⭐⭐ | 高 | 渐进式迁移，每步验证收益和代价 |

### 9.2 复杂度控制策略

1. **单一职责**：每个 Skill 只做一件事
2. **声明式配置**：减少隐式依赖
3. **渐进式迁移**：每阶段完成后验证
4. **可回退设计**：每个阶段都有 Feature Flag
5. **持续重构**：迁移过程中清理旧代码

---

## 附录：目录结构变化

### 重构前

```
backend/app/
├── ai/                    # AI 核心逻辑
│   ├── alarm.py
│   ├── chat_chain.py
│   ├── decider.py
│   ├── executor.py
│   ├── llm.py
│   └── response_worker.py
├── chat/                  # 聊天模块（新增）
│   ├── __init__.py
│   ├── connection.py
│   ├── delivery.py
│   ├── dm.py
│   └── message.py
├── services/              # 服务层
│   ├── action_decider.py (兼容层)
│   ├── ai_response_worker.py (兼容层)
│   ├── event_bus.py
│   ├── group_service.py (兼容层)
│   ├── llm_service.py (兼容层)
│   ├── skill_engine.py
│   └── skill_service.py
└── tools/                 # 工具实现
    ├── chat_social/
    ├── file_operations/
    ├── memory/
    ├── network/
    ├── self_config/
    └── self_management/
```

### 重构后（目标）

```
backend/app/
├── ai/                    # 极薄大脑
│   ├── executor.py
│   ├── llm.py
│   └── response_worker.py (< 300 行)
├── chat/                  # 聊天底层服务
│   ├── __init__.py        # ChatApi
│   ├── protocol.py        # BaseChatApi ABC
│   ├── connection.py
│   ├── delivery.py
│   ├── dm.py
│   └── message.py
├── services/              # 基础设施服务
│   ├── conflict_arbiter.py
│   ├── event_bus.py
│   ├── heartbeat_manager.py
│   ├── resource_manager.py
│   ├── skill_event_bus.py
│   ├── template_engine.py
│   └── trigger_engine.py
├── skills/                # Skill 层（新增）
│   ├── __init__.py
│   ├── adapters.py        # 遗留技能适配器
│   ├── base.py            # AutonomousSkill 基类
│   ├── app_skills/        # App Skill 实现
│   │   ├── chat_chain_skill.py
│   │   ├── decision_skill.py
│   │   ├── subscribe_event.py
│   │   └── update_attention.py
│   └── state_skills/      # State Skill 实现
│       ├── alarm_skill.py
│       └── memory_skill.py
└── tools/                 # 工具实现（保留）
    ├── chat_social/
    ├── file_operations/
    ├── memory/
    ├── network/
    ├── self_config/
    └── self_management/
```

---

## 附录：Feature Flag 管理

所有新功能通过 Feature Flag 控制，默认关闭，逐步启用。

```python
# app/config.py

class Settings(BaseSettings):
    # ... 现有配置 ...
    
    # Phase 1
    enable_chatapi_v2: bool = False
    
    # Phase 2
    enable_autonomous_skill: bool = False
    
    # Phase 3
    enable_trigger_engine: bool = False
    
    # Phase 4
    enable_thin_brain: bool = False
    
    # Phase 5
    enable_template_system: bool = False
```

---

## 附录：迁移优先级

| 优先级 | 任务 | 原因 |
|--------|------|------|
| P0 | Phase 2: AutonomousSkill 基类 | 整个重构的基石 |
| P0 | Phase 2: SkillEventBus | Skill 间通信的核心 |
| P1 | Phase 1: ChatApi 完善 | 为后续拆分铺路 |
| P1 | Phase 3: TriggerEngine | 多维触发是核心能力 |
| P1 | Phase 4: ConflictArbiter | 多 Skill 并发必需 |
| P2 | Phase 4: ResourceManager | 资源竞争控制 |
| P2 | Phase 4: PersonalityAnchor | 人格一致性 |
| P3 | Phase 5: TemplateEngine | 生态建设 |